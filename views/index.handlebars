<div class="container-fluid url-shortener d-flex flex-column align-items-center">
  <h3 class="title mb-3">輸入想要縮短的網址：</h3>
  <form class="d-flex flex-row" id="form" method="POST" action="/record" novalidate>
    <div class="mb-3 me-3 url-input">
      <input type="url" class="form-control mb-3" {{#if url}}value="{{url}}" {{/if}} placeholder="https://example..."
        name="url" required>
      <div class="d-flex flex-column align-items-start short-url-block">
        {{#if shortened_url}}
        <div class="m-o p-0">
          <span class="result">短網址：</span>
          <span class="result text-area">{{mainURL}}{{shortened_url}}</span>
        </div>
        <button class="btn-copy mt-2" onclick="copyToClipBoard()">複製</button>
        {{/if}}
      </div>
    </div>
    <div class="d-flex flex-column confirm-block">
      <button type="submit" class="btn btn-confirm mb-3">確定</button>
    </div>
  </form>
</div>